apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "plweb.fullname" . }}-test-connection"
  labels:
    {{- include "plweb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "plweb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
